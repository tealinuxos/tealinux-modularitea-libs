
#[derive(Clone, Copy, Debug)] // soon, add serialize here. because
pub struct GrubChangerContext {
    // fields
}


impl GrubChangerContext {
    pub fn new(theme: String)  -> Self {
        Self {}
    }

    pub fn apply(self) {
        unimplemented!();
    }

    pub fn private_func_arrsource() {
        // place all kind of theme, or anything stuff need to be copied here.
    }
}